package texp_test

// func TestWithDefault(t *testing.T) {
// 	expect := texp.Expect(t)

// 	if c := expect(nil).Config(); !reflect.DeepEqual(c, expC) {
// 		t.Errorf("Wrong configuration. exp %v, got %v", expC, c)
// 	}
// }
